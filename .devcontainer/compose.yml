# create the network before building the devcontainer
# docker network create licodex
#
# build base, tools and devcontainer images
# USRID=$(id -u) USRNAME=$(whoami) docker compose -f .devcontainer/compose.yml build --no-cache --pull=false
#
# >Dev Containers: Reopen in Container
services:
  # Base development image with system dependencies
  base:
    build:
      context: ..
      dockerfile: .devcontainer/docker/Dockerfile.base
    image: ghcr.io/diogobaltazar/licodex-devcontainer-base:1.0.0

  # Tools image with Docker, Node.js, and CDK
  tools:
    build:
      context: ..
      dockerfile: .devcontainer/docker/Dockerfile.tools
    image: ghcr.io/diogobaltazar/licodex-devcontainer-tools:1.0.0
    depends_on:
      - base

  # devcontainer image for development
  devcontainer:
    build:
      context: ..
      dockerfile: .devcontainer/docker/Dockerfile
      args:
        USRID: ${USRID}
        USRNAME: ${USRNAME}
    image: ghcr.io/diogobaltazar/licodex-devcontainer:1.0.0
    container_name: licodex-devcontainer
    depends_on:
      - tools
