# compose project name #########################################################
#
# Using devcontainer, the project opens under your user name, which is what compose
# takes as the project name. This can cause issues with dependencies on other
# containers.
#
name: versionminus
services:
  # BACKEND ####################################################################
  milvus-etcd:
    container_name: versionminus-milvus-etcd
    image: quay.io/coreos/etcd:v3.5.0
    environment:
      - ETCD_AUTO_COMPACTION_MODE=revision
      - ETCD_AUTO_COMPACTION_RETENTION=1000
      - ETCD_QUOTA_BACKEND_BYTES=4294967296
      - ETCD_SNAPSHOT_COUNT=50000
    volumes:
      - versionminus-milvus-etcd:/etcd
    # IMPORTANT: advertise a host reachable by other containers (NOT 127.0.0.1)
    # If we advertise 127.0.0.1 Milvus will follow redirects to its own loopback and fail.
    command: etcd -advertise-client-urls=http://milvus-etcd:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
    healthcheck:
      test: ["CMD", "etcdctl", "--endpoints=http://127.0.0.1:2379", "endpoint", "health"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - versionminus

  milvus-minio:
    container_name: versionminus-milvus-minio
    image: minio/minio:RELEASE.2023-03-20T20-16-18Z
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - versionminus-milvus-minio:/data
    command: minio server /data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - versionminus

  milvus:
    build:
      context: .
      dockerfile: src/versionminus/milvus/Dockerfile
    image: ghcr.io/versionminus/versionminus-milvus:1.0.0
    container_name: versionminus-milvus
    environment:
      # Use explicit service names (no network aliases required)
      # Service 'milvus-etcd' exposes client port 2379
      ETCD_ENDPOINTS: milvus-etcd:2379
      # Service 'milvus-minio' exposes port 9000
      MINIO_ADDRESS: milvus-minio:9000
      DEPLOYMENT_ENV: ${DEPLOYMENT_ENV:-}
    volumes:
      - versionminus-milvus:/var/lib/milvus
    ports:
      - "19530:19530" # grpc, http
      - "9091:9091" # monitoring
    depends_on:
      milvus-etcd:
        condition: service_healthy
      milvus-minio:
        condition: service_started
    networks:
      - versionminus

  db:
    build:
      context: .
      dockerfile: src/versionminus/db/Dockerfile
    image: versionminus-db:1.0.0
    container_name: versionminus-db
    restart: unless-stopped
    environment:
      # Postgres image consumes these; defaults also baked into Dockerfile but we keep explicit for clarity
      POSTGRES_USER: versionminus
      POSTGRES_PASSWORD: versionminuspwd
      POSTGRES_DB: versionminus
    volumes:
      - versionminus-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 10
    # IMPORTANT: place db on the same user-defined network as api so hostname 'db' resolves inside api container
    networks:
      - versionminus

  api:
    build:
      context: .
      dockerfile: src/versionminus/api/Dockerfile
    image: versionminus-api:1.0.0
    container_name: versionminus-api
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
      mcp:
        condition: service_started
      otel-collector:
        condition: service_started
    ports:
      - "8000:8000"
    environment:
      - MODELHUB_API_KEY
      - MODELHUB_BASE_URL
      - MODELHUB
      - OTEL_SERVICE_NAME=versionminus-otel-collector
      - OTEL_EXPORTER_OTLP_ENDPOINT=http://versionminus-otel-collector:4318
      - OTEL_TRACES_SAMPLER=parentbased_always_on
      - OTEL_EXPORTER_OTLP_INSECURE=true
    env_file:
      - .env
    networks:
      - versionminus

  mcp:
    build:
      context: .
      dockerfile: src/versionminus/mcp/Dockerfile
    image: versionminus-mcp:1.0.0
    container_name: versionminus-mcp
    restart: unless-stopped
    environment:
      - CHUNK_POLICY_MODEL_PATH
    networks:
      - versionminus

  # FRONTEND ###################################################################
  web:
    build:
      context: .
      dockerfile: src/versionminus/client/web/Dockerfile
    image: versionminus-web:1.0.0
    container_name: versionminus-web
    restart: unless-stopped
    depends_on:
      - api
    ports:
      - "5173:80"
    networks:
      - versionminus

  # OBSERVABILITY ##############################################################
  loki:
    image: grafana/loki:2.9.4
    container_name: versionminus-loki
    command: -config.file=/etc/loki/config.yml
    volumes:
      - ./src/versionminus/observability/loki/config.yml:/etc/loki/config.yml:ro
      - versionminus-loki-data:/loki
    ports:
      - "3100:3100"
    networks:
      - versionminus

  # Log forwarder/shipper
  #
  # Tails Docker container logs on the host, parses JSON, adds basic labels, and
  # pushes logs into Loki for centralized search and retention.
  fluent-bit:
    image: fluent/fluent-bit:2.1.8
    container_name: versionminus-fluent-bit
    environment:
      DEPLOYMENT_ENV: ${DEPLOYMENT_ENV:-local}
    volumes:
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./src/versionminus/observability/fluent-bit/fluent-bit.conf:/fluent-bit/etc/fluent-bit.conf:ro
      - ./src/versionminus/observability/fluent-bit/parsers.conf:/fluent-bit/etc/parsers.conf:ro
    depends_on:
      - loki
    networks:
      - versionminus

  # Distributed tracing backend
  #
  # Stores traces sent via OTLP (through the OTEL Collector). Lets you view
  # request spans/timelines in Grafana and correlate them with logs/metrics.
  tempo:
    image: grafana/tempo:2.2.2
    container_name: versionminus-tempo
    command: -config.file=/etc/tempo/tempo.yml
    volumes:
      - ./src/versionminus/observability/tempo/tempo.yml:/etc/tempo/tempo.yml:ro
      - versionminus-tempo-data:/var/tempo
    ports:
      - "3200:3200"
    networks:
      - versionminus

  # Vendor‑neutral telemetry router
  #
  # Receives OTLP data (traces and custom metrics) from the API SDKs/clients,
  # batches and exports traces to Tempo and metrics to Prometheus (and logs to
  # its console for debugging).
  otel-collector:
    image: otel/opentelemetry-collector:0.93.0
    container_name: versionminus-otel-collector
    command:
      - "--config=/etc/otelcol/config.yaml"
    volumes:
      - ./src/versionminus/observability/otel-collector:/etc/otelcol:ro
    ports:
      - "4317:4317"
      - "4318:4318"
      - "8889:8889"
    depends_on:
      - tempo
    networks:
      - versionminus

  # Metrics collection and storage
  #
  # Scrapes /metrics endpoints (API, Milvus, OTEL collector), stores time series
  # and powers Grafana metric queries and alerting rules.
  prometheus:
    image: prom/prometheus:v2.48.0
    container_name: versionminus-prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--web.enable-lifecycle"
    volumes:
      - ./src/versionminus/observability/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - versionminus-prometheus-data:/prometheus
    ports:
      - "9090:9090"
    depends_on:
      - otel-collector
    networks:
      - versionminus

# Visualization and dashboards
#
# Connects to Prometheus (metrics), Loki (logs), and Tempo (traces). Use it to
# build panels, explore queries, and correlate logs/metrics/traces via labels
# like trace_id
  grafana:
    image: grafana/grafana:10.2.3
    container_name: versionminus-grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_AUTH_ANONYMOUS_ENABLED: "true"
    volumes:
      - versionminus-grafana-data:/var/lib/grafana
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
      - loki
      - tempo
    networks:
      - versionminus

volumes:
  versionminus-db-data:
    name: versionminus-db-data
  versionminus-milvus:
    name: versionminus-milvus
  versionminus-milvus-minio:
    name: versionminus-milvus-minio
  versionminus-milvus-etcd:
    name: versionminus-milvus-etcd
  versionminus-loki-data:
    name: versionminus-loki-data
  versionminus-prometheus-data:
    name: versionminus-prometheus-data
  versionminus-tempo-data:
    name: versionminus-tempo-data
  versionminus-grafana-data:
    name: versionminus-grafana-data

networks:
  versionminus:
    name: versionminus
    external: true
