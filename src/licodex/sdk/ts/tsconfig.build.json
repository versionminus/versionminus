{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Force true ESM output so bundlers (Vite) can statically analyze named exports.
    // This avoids the previous situation where the ESM build looked like CJS and
    // resulted in runtime "Importing binding name 'useLicodex' is not found" errors.
    "module": "ESNext",
    "moduleResolution": "Node",
    "emitDeclarationOnly": false,
    "declaration": true,
    "declarationMap": true
  }
}
